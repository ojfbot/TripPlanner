/**
 * UI constants for consistent timing and positioning
 */

/**
 * Animation and interaction delays
 */
export const FOCUS_DELAY_MS = 100;
export const SEND_ANIMATION_DELAY_MS = 300;

/**
 * UI text constants
 */
export const UNREAD_BADGE_TEXT = 'new';

/**
 * NOTE: Layout constants (dashboard margins, sidebar width, chat dimensions, etc.)
 * are defined as CSS custom properties in styles/variables.css and used directly in CSS.
 * This ensures a single source of truth for layout values.
 */
